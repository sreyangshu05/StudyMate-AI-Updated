# Frontend environment. Copy to .env and adjust for your deployment.
# VITE_ vars are inlined at build time by Vite; never put secrets here.

# Base URL of the StudyMate backend API.
# Defaults to the local development backend if unset.
VITE_API_BASE_URL=http://localhost:5000/api
